name = "s4hs"
compatibility_date = "2026-06-19"

# Static site (no Worker script) — deploy the built `dist/` as Workers Assets.
# CI: build command `npm run build`, deploy command `npx wrangler deploy`.
[assets]
directory = "./dist"
