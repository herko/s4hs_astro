name = "s4hs"
pages_build_output_dir = "dist"

# Cloudflare Pages static site. Build with `npm run build`, then publish `dist/`
# (Pages Git build, or `npx wrangler pages deploy dist`). Custom domains can be
# attached via an external DNS CNAME/ALIAS, so the zone need not be on Cloudflare.
